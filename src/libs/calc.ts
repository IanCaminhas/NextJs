//simulação de uma bibloiteca de cálculo: soma e subtração.
//Imagine algo enorme que tenho que carregar por demanda
export default {
    sum: (x: number, y:number) =>{
        return x + y;
    },
    sub: (x: number, y:number) =>{
        return x - y;
    },
}